<div class="form">
<h1><%= @pin.title %></h1>

<p><%= image_tag @pin.image.url(:large) %><p>
<h4><%= @pin.description %></h4>
<%= link_to "Edit", edit_pin_path %> 
<%= link_to "Delete", pin_path, method: :delete, data: {confirm: "Are you sure?"} %>
<br>
<br>
<h4>Comments</h4>
<%= render @pin.comments %>
<%= render 'comments/form' %>
</div>
